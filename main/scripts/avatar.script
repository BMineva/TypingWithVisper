function init(self)
	--create the avatar using a factory

	
end

function on_message(self, message_id, message, sender)
	if message_id == hash("spawn") then
		print("message recieved")
		character = "#visper"
		factory.create(character)
		print(character)
	end

end